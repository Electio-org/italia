[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lce-loader"
version = "0.11.0"
description = "Official Python loader for Electio Italia bundles"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["pandas>=2.0"]
